# ==========================================
# audio2sub - 核心依赖清单
# ==========================================

# 1. 语音转写与推理引擎 (Faster-Whisper / CTranslate2)
faster-whisper>=1.0.0
ctranslate2>=4.0.0
numpy>=1.22.0


# ==========================================
# 可选依赖 (NVIDIA GPU CUDA 12 运行支持)
# ==========================================
# 如果在 Windows + NVIDIA 显卡环境下遇到 cublas64_*.dll 缺失：
# pip install nvidia-cublas-cu12
